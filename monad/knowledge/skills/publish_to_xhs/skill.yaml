name: publish_to_xhs
goal: 将文章/笔记发布到小红书平台（图文笔记），支持自动生成知识图谱配图
inputs:
- title (标题，≤20字)
- content (正文内容)
- topics (话题标签列表，如 ["AI", "技术分享"])
- images (图片路径列表，可选)
- knowledge_map (是否自动生成知识图谱配图，默认 True)
- source_url (原文链接，会追加到正文末尾)
steps:
- 若无图片，走"文字配图"模式：用 LLM 将正文精炼为 6-8 行关键信息生成文字卡片
- 若有图片，直接上传图片
- 若开启 knowledge_map，调用 doc2mermaid 生成知识图谱 PNG 并作为额外图片上传
- 填写标题（自动截断到 20 字）
- 在正文编辑器中通过 #关键词 添加话题标签
- 点击发布
notes:
- 文字卡片不宜过长，LLM 会自动把正文提炼为 6-8 行精华（每行 ≤20 字）
- 首次使用需运行 login() 登录，使用持久化浏览器 profile 保存登录态
- 知识图谱需要 doc2mermaid 包和 mmdc (Node ≥18)
triggers:
- 用户要求把文章或 blog 发到小红书
- 用户要求发布小红书笔记
- 发布内容到小红书
- 帮我发小红书
dependencies:
  python:
    - doc2mermaid
    - playwright
  system:
    - mmdc  # npm install -g @mermaid-js/mermaid-cli
